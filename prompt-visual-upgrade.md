# Промпт по візуалу та анімаціях для Claude Code

Скопіюй і дай Claude Code разом з файлом `visual-reference.html`.

---

## ПРОМПТ:

Я прикріпляю файл `visual-reference.html` — це повний візуальний референс для сайту SAV.AGENCY з усіма WOW-ефектами які потрібно реалізувати. Відкрий його і вивчи код — всі ефекти робочі і ти можеш побачити як вони реалізовані.

Перепиши мій поточний `index.html` повністю, використовуючи візуальні ефекти і анімації з референсу. Ось конкретний перелік того що потрібно імплементувати:

### 🎨 ФОНОВІ ЕФЕКТИ:

1. **Mesh Gradient Background** — замість простого radial-gradient зроби 4 окремих "орба" (кольорових плям) які плавно рухаються по екрану (`float` анімація з різними `animation-delay` і `animation-duration`). Кольори: indigo, violet, pink, cyan. Blur 80px, opacity 0.4. Це створює живий, дихаючий фон замість статичного.

2. **Grid Lines Overlay** — тонкі лінії-сітка (80x80px) з opacity 0.02 які видно тільки в центрі екрану (через `mask-image: radial-gradient`). Це додає глибину і технологічний вайб.

3. **Noise Texture** — SVG noise як є, але тонший (opacity 0.035 замість 0.07).

4. **Floating Particles** — 30 маленьких точок (1-3px) які повільно піднімаються знизу вгору за hero секцією. JS генерує їх рандомно з різною швидкістю і затримкою.

### 🖱️ КАСТОМНИЙ КУРСОР:

5. **Custom Cursor** — два елементи: зовнішнє кільце (20px, border, плавно слідує за мишкою з затримкою через `requestAnimationFrame`) і внутрішня точка (5px, миттєво слідує). При наведенні на інтерактивні елементи (кнопки, карточки, лінки) кільце збільшується до 50px і стає фіолетовим. На мобільних — прихований. Це одразу створює premium відчуття.

### ✨ HOVER ЕФЕКТИ НА КАРТКАХ:

6. **Mouse-tracking Glow** — на кожній service карточці при русі мишки по ній з'являється radial-gradient що слідує за курсором (через CSS custom properties `--mouse-x` і `--mouse-y` які оновлюються через JS `mousemove`). Два шари: заливка (opacity 0.12) і бордер (окремий pseudo-element). Це робить кожну карточку "живою".

### 📝 ТИПОГРАФІКА:

7. **Шрифти** — замінити Inter та Outfit на:
   - `Syne` (800 weight) для заголовків — жирний, характерний, з geometric формами
   - `Space Mono` для цифр і тегів — monospace дає tech-вайб
   - `DM Sans` (300, 400, 500) для body тексту — чистий і сучасний

8. **Gradient Text** на головному заголовку hero — `background: linear-gradient(135deg, #fff, #c7d2fe, #a78bfa, #818cf8, #fff)` з `background-size: 300%` і анімація `gradientShift` яка плавно зсуває градієнт. Виглядає як голографічний ефект.

### 🎬 АНІМАЦІЇ:

9. **Staggered Reveal** — елементи в grid (карточки, pricing, process steps) появляються не одночасно а з затримкою 0.1s між кожним. Реалізувати через клас `.stagger-in` на контейнері з `:nth-child()` CSS transitions з різними `transition-delay`.

10. **Count-Up Numbers** — цифри в Social Proof Bar рахують від 0 до цільового числа за 2 секунди при появі в viewport (Intersection Observer + JS setInterval). Атрибут `data-target="15"` задає цільове число.

11. **Scroll-triggered Reveal** — всі секції появляються при скролі через Intersection Observer з `threshold: 0.15` і `rootMargin: '0px 0px -50px 0px'`. Початковий стан: `opacity: 0; transform: translateY(30px)`. Анімація через CSS transition з `cubic-bezier(0.16, 1, 0.3, 1)` — це дає пружний, приємний рух.

12. **Scroll Indicator** — внизу hero секції маленька вертикальна лінія з анімованою крапкою що рухається вниз (keyframe `scrollDown`). Зникає при скролі.

13. **Button Shine Effect** — на primary CTA кнопках при hover проїжджає горизонтальний "блік" (pseudo-element `::before` з `linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent)` який рухається від left:-100% до left:100%).

### 🏗️ КОМПОНЕНТИ:

14. **Sticky Header** — при скролі > 50px header отримує `backdrop-filter: blur(20px) saturate(1.5)`, напівпрозорий фон і border-bottom. Плавна transition.

15. **Hero Badge** — маленький pill-badge зверху з пульсуючою зеленою точкою (`pulse` анімація на scale і opacity). Текст: "AI-Powered Marketing Intelligence".

16. **Section Tags** — перед кожним заголовком секції монospaced тег типу `// Послуги` кольору accent.

17. **Nav Link Underline** — при hover на навігаційних лінках з'являється підкреслення знизу (pseudo-element `::after`, width від 0 до 100% через transition).

18. **FAQ Accordion** — клік на питання відкриває відповідь (max-height від 0 до 200px), іконка "+" повертається на 45° стаючи "×". Тільки одне питання відкрите одночасно.

19. **Pricing Card Featured** — середня карточка pricing трохи більша (scale 1.02), з фіолетовим бордером і glow-лінією зверху (gradient pseudo-element).

### 🎨 КОЛЬОРОВА СХЕМА:

```css
--bg: #050508;
--surface: rgba(255,255,255,0.03);
--border: rgba(255,255,255,0.08);
--border-hover: rgba(255,255,255,0.2);
--text: #f0f0f5;
--text-muted: #6b7094;
--accent: #6366f1;           /* indigo — основний акцент */
--accent-light: #818cf8;
--accent-glow: rgba(99, 102, 241, 0.4);
--green: #34d399;            /* для цін і "check" іконок */
--amber: #fbbf24;            /* для badges */
--rose: #fb7185;             /* для проблем/pain points */
```

### ⚠️ ВАЖЛИВО:

- Всі ефекти повинні бути в ОДНОМУ index.html файлі (CSS і JS вбудовані)
- Всі анімації мають бути performant (використовуй `transform` і `opacity`, уникай анімації `width`, `height`, `margin`)
- Mobile responsive: кастомний курсор прихований, grid стає 1 колонкою, header compact
- Контент і структура сайту — з прикріпленого `visual-reference.html`, не міняй тексти
- Cubic-bezier `(0.16, 1, 0.3, 1)` — використовуй скрізь для природних, пружних анімацій
- Кнопка CTA повинна мати `box-shadow: 0 0 30px var(--accent-glow)` — це створює glow ефект
- Фон карточок — glassmorphism: `backdrop-filter: blur(20px)` + напівпрозорий background

Бери `visual-reference.html` як повний робочий приклад і імплементуй ВСЕ в мій проект.
